/** DataSonnet
version=3.0
default="OK"
*/

{
  tryNonexistent: payload.doesntExist,
  tryObj: {
    x: payload.none
  },
  tryOverride: payload.override default "OverrideOK",
  tryElseOverride: try payload.override else "OverrideOK",
}
