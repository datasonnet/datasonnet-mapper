local params = {
    "NamespaceDeclarations" : {
        "datasonnet" : "http://www.modusbox.com"
    },
    "NamespaceSeparator": "%",
    "TextValueKey": "__text",
    "AttributeCharacter": "*",
    "XmlVersion" : "1.0"
};

//local converted = DS.Formats.writeExt(payload, "application/xml", params);
local converted = DS.Formats.write(payload, "application/xml", params);

converted
