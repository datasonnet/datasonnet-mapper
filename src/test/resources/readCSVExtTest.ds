local params = {
    "UseHeader": false,
    "Quote": "'",
    "Separator": "|",
    "Escape": "\\",
    "NewLine": "\n"
};
//local csvFile = PortX.Formats.readExt(payload, "application/csv", params);
local csvFile = PortX.Formats.read(payload, "application/csv", params);

{
    fName: csvFile[0][0],
    num: csvFile[0][3]
}
